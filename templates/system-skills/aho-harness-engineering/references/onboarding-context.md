# Onboarding And Audit Context

Use existing project-state, memory-layout, and document-generation references to produce the typed result defined in `output-contract.md`. Set `context` with project state, uncertainty, and recommendations; use only envelope target IDs for any ready patch.

For `onboard`, identify the smallest project-specific context still needed after deterministic Runtime preparation. For `audit`, report drift and uncertainty without creating lifecycle work. Questions, confirmations, Change creation, and next-action routing belong to the caller, not this Skill.

Do not turn onboarding into ordinary feature planning, source implementation, worker scheduling, or maintenance/evolution triggering.
