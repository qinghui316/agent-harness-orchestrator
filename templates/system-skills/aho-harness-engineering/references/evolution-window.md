# Assigned Evolution Window

Analyze exactly the fixed five-close archive window supplied by Runtime. Do not count Changes or inspect `pending.md` to select, expand, or redefine the window.

Compare repeated failures, user corrections, validation gaps, mechanical-rule opportunities, and current documentation drift against canonical Markdown. Classify each candidate as Promote, Retain, Merge, Retire, or Archive-only.

First produce a concrete read-only proposal, then native-spawn one independent scorer child. Only a score of at least 80 with no hard issue allows direct edits to canonical target docs. Prefer consolidating, splitting, renaming, or deleting stale guidance over appending phase history. A valid accepted result may leave every file unchanged after both new-rule gaps and old-experience retention are checked.
