display_name: AHO Harness Engineering
short_description: Reconcile an assigned project Harness from evidence.
default_prompt: >-
  Use the AHO task facts and matching mode. Detect the project's actual Harness,
  compute the evidence-backed delta, edit directly when justified, verify it, and
  require native scoring before Evolution edits.
