# Review: workbench-usable-manual-closed-loop

Status: completed.

## Findings

No unresolved findings.

- Subagent independent review found that `result.apply` auto-finalized/archived the Change, violating the human close/archive gate. Fixed by removing apply-time auto-close from `src/server/workbench/actions.ts` and proving explicit `change.close` remains a separate confirmation.
- Subagent review noted the DOM test is useful but should not be the only proof. Fixed by making the slow Workbench apply integration flow the golden loop proof and keeping the DOM test as visible-surface honesty coverage.
- Subagent review requested edge coverage for result-review apply de-duplication. Fixed with a read-model unit test that suppresses only the matching `changeId:worktreeId` apply approval and preserves other apply targets.

## Verification

- Selected verification scope: targeted Workbench read-model/server/DOM/slow apply coverage, product checks, and Harness checks.
- Targeted suites passed:
  - `npx vitest run tests/unit/workbench-read-model.test.ts --reporter=dot`
  - `npx vitest run tests/unit/workbench-server.test.ts --reporter=dot`
  - `npx vitest run tests/unit/web-app.test.tsx --reporter=dot`
  - `npx vitest run tests/slow/workbench-apply-integration-flow.test.ts --reporter=dot`
- Product checks passed:
  - `npm run typecheck`
  - `npm run lint`
  - `npm run test:fast`
  - `npm run build`
- Retry/environment notes: first project-level `npm run lint` failed on a mechanical `prefer-const` issue introduced while removing auto-close; fixed by changing the apply result binding to `const`, then `npm run lint` passed.
- Full / aggregate suites run or skipped: `npm run test:fast` covered broad unit coverage; the touched Workbench aggregate owner paths were covered by targeted read-model/server/DOM suites plus the slow apply integration flow. Full `npm run test` was not run because this slice did not touch release packaging, remote provider execution, or all slow Workbench flows beyond the apply/integration suite.
- Rationale for selected scope: the changed behavior is bounded to Workbench projection ordering, result-review apply de-duplication, server action finalization semantics, source apply safety, and close/handoff docs.

## Acceptance Feedback

- Real/manual acceptance performed: automated Workbench acceptance through the slow integration fixture; no browser screenshot was needed because the golden flow uses the Workbench action/read-model path and the DOM test covers visible surface rendering.
- Manual config edits: none.
- Extra prompts or reviewer instructions: subagent independent review scoped to boundary, docs drift, and test coverage.
- Retries or environment failures: one lint retry after the `prefer-const` fix.
- Screenshots / artifacts / run ids: Vitest command output recorded in terminal; no persistent run id generated by the test runner.
- External source/state safety: slow Workbench apply integration fixture confirmed clean source status before explicit `result.apply`, clean source status after apply with commit, and no archive until explicit `change.close`.
- Remote handoff acceptance: not applicable; remote handoff is out of scope.
- Product-fixable workarounds or follow-up evidence: none required for the manual loop; full-auto remains later-roadmap.

## Documentation Entropy Coverage

- Documentation entropy coverage applicable: yes.
- Documents checked: `AGENTS.md`, `docs/STATUS.md`, `docs/CURRENT-DEVELOPMENT-PLAN.md`.
- Before/after line counts: `AGENTS.md` stayed compact at 159 lines; `docs/STATUS.md` was compressed from a long historical handoff to 112 lines; `docs/CURRENT-DEVELOPMENT-PLAN.md` is 97 lines and carries current roadmap context.
- Duplicate current-state fields checked: current handoff fields are concentrated in `AGENTS.md` and `docs/STATUS.md`; plan-level narrative stays in `docs/CURRENT-DEVELOPMENT-PLAN.md`.
- Roadmap/current-direction stale language checked: `rg` found no stale immediate full-auto / old controlled Scheduler next-step phrases in the three handoff docs.
- Archive-ledger content promoted / retained / merged / retired / archive-only: current Workbench manual-loop direction was promoted; full-auto was retained as later-roadmap; controlled Scheduler history was merged into a short baseline and left archive-only for detail.
- Over-budget documents and rationale: none for this slice; `docs/STATUS.md` is no longer acting as a phase ledger.
- Tested with: stale phrase grep and final Harness lint.

## Experience Lifecycle Coverage

- Experience lifecycle coverage applicable: yes.
- Promote decisions: Workbench main-surface manual-gated closed loop is promoted as the current product usability baseline.
- Retain decisions: controlled Scheduler continuation remains useful bounded gate capability; full-auto remains a later product option.
- Merge decisions: repeated controlled Scheduler historical paragraphs were merged into a short baseline and archive lookup.
- Retire decisions: immediate next-step language pointing at full-auto or more scheduler evidence was retired.
- Archive-only decisions: detailed phase chronology stays in archived summaries and `harness/changes/INDEX.json`.
- Noop / no-change rationale after old-experience scan: no Harness rule/template change was needed; this was a product path and handoff convergence change.
- Tested with: handoff grep, `lint-ecl`, and `harness-evolve check`.

## Worktree Diff Artifact Coverage

- New-file / untracked worktree diff coverage applicable: partially; this change uses existing apply/worktree diff behavior but does not alter diff collection.
- Tested with: `tests/slow/workbench-apply-integration-flow.test.ts`, including apply after reviewed worktree changes and source-clean before/after evidence.
- If not applicable, reason: no new diff format, untracked-file collector, or worktree artifact schema was introduced.

## Read Model Projection Coverage

- Workbench / GUI read-model projection coverage applicable: yes.
- Checked scope: result-review apply de-duplication, selected-demand confirmation queue primary ordering, close-gate promotion after apply, and visible result/validation/audit state.
- Tested with: `tests/unit/workbench-read-model.test.ts`, `tests/unit/web-app.test.tsx`, and `tests/slow/workbench-apply-integration-flow.test.ts`.

## Workbench User-Surface Honesty Coverage

- Workbench user-surface honesty coverage applicable: yes.
- Sampled surface: parent transcript, result-review tab/card, right confirmation queue, and action buttons.
- Visible primary UI backed by implemented workflow paths: apply is backed by `result.apply`; close is backed by `change.close`; validation/audit evidence comes from real stored artifacts in the slow flow.
- Out-of-scope future capability check: default surface does not show fake full-auto, parallel executor, merge queue, slot allocator, or whole-wave controls.
- Forbidden visible internal terms/actions checked: primary action copy avoids future capability promises and keeps internal terms in evidence/detail contexts.
- Duplicate primary action check: result-review apply de-duplicates the matching approval item; confirmation queue exposes one primary gate for the selected demand.
- High-impact action path result: apply requires confirmation and does not archive; close/archive requires a separate confirmed `change.close`.
- Real App DOM / browser UI verification result when the behavior is product-visible: `tests/unit/web-app.test.tsx` covers the rendered Workbench surface.
- Projection/unit evidence that supplements but does not replace visible-surface acceptance: read-model and server tests cover target ids and fail-closed boundaries; slow integration proves the real loop.
- Tested with: targeted Workbench suites listed above.

## Scoped Workbench Action Payload Coverage

- Scoped Workbench action payload coverage applicable: yes.
- Checked target ids: `result.apply` carries `changeId` and `worktreeId`; `change.close` carries the explicit target `changeId`.
- Tested action path: server forwards scoped action args unchanged and fails closed for missing/stale scoped targets.
- Duplicate action/evidence affordance check: matching apply approval is suppressed when the result-review apply gate already represents the same target; different worktree/change targets remain visible.
- Tested with: `tests/unit/workbench-server.test.ts`, `tests/unit/workbench-read-model.test.ts`, and `tests/slow/workbench-apply-integration-flow.test.ts`.

## Transcript Renderer Source-Boundary Coverage

- Transcript renderer source-boundary coverage applicable: yes.
- Canonical transcript projection checked: main conversation displays the user demand and assistant/result summary.
- Assistant markdown source checked: existing renderer behavior is unchanged.
- Process/tool row compactness checked: unchanged by this slice.
- Derived workflow summary exclusion checked: UI test confirms result evidence appears in result-review/detail surfaces instead of becoming a fake primary workflow command.
- Worker/role transcript scoping checked: selected demand scope is maintained in the slow flow while another demand remains active.
- Private chain-of-thought exclusion checked: no hidden reasoning source was added.
- Tested with: `tests/unit/web-app.test.tsx` and `tests/slow/workbench-apply-integration-flow.test.ts`.

## Source Apply Safety Coverage

- Source apply safety coverage applicable: yes.
- Checked source project / fixture: isolated temporary package fixture created by the slow Workbench apply integration test.
- Checked worktree ids / result ids / integration check ids: reviewed worktree id is asserted in the rendered `result.apply` action args; integration check behavior remains covered by the same slow suite.
- Source-root mutation gate checked: source status remains clean before explicit human-confirmed `result.apply`; apply requires `confirm: true`; close/archive is not triggered by apply.
- Out-of-scope source mutation check: remote push/PR/merge and Harness evolution apply are out of scope and not exposed by the loop.
- Tested with: `tests/slow/workbench-apply-integration-flow.test.ts`.

## Runtime Bridge Boundary Coverage

- Runtime bridge boundary coverage applicable: yes.
- Checked boundary: Workbench server action handler now returns apply results without creating a close/archive runtime transition; canonical close remains the existing `change.close` action.
- Tested with: `tests/unit/workbench-server.test.ts` and `tests/slow/workbench-apply-integration-flow.test.ts`.

## Proposal / Runtime Boundary Coverage

- Proposal/runtime boundary coverage applicable: yes.
- Artifact type and authority classification: Workbench projections and result-review gates remain derived UI/read-model artifacts; Change/apply/close records remain workflow truth.
- Boundary matrix checked: no new authority, evidence family, or execution path was introduced.
- Out-of-scope execution paths checked: no full-auto, scheduler full loop, remote merge/push, or Harness evolution apply behavior added.
- Stale/forged target behavior checked: existing server tests cover stale/missing scoped targets; slow integration covers forged integration targets.
- Tested with: targeted Workbench server/read-model/slow suites.

## Goal Loop Boundary Coverage

- Goal Loop boundary coverage applicable: yes, as a regression boundary only.
- Persistent Goal/Change scope checked: no Goal Loop execution authority was added.
- Recommendation authority checked: Goal Loop remains evidence/context only.
- Fallback priority checked: unchanged.
- Packet / main-Agent context freshness checked: unchanged by this slice.
- Stale or superseded packet suppression checked: unchanged by this slice; broad Goal Loop tests passed under `npm run test:fast`.
- Feedback selected Change / packet lineage / visible gate scope checked: unchanged.
- Feedback remains user evidence, not hidden instruction / arbitrary chat scrape / execution approval: unchanged.
- Feedback-triggered re-evaluation remains non-executing and primary Harness gate stays separate: unchanged.
- Hidden execution / source mutation check: no Goal Loop path mutates source.
- ToolPolicyGate / human gate preservation checked: apply and close remain human-confirmed concrete actions.
- Tested with: `npm run test:fast` and targeted Workbench suites.

## Module Boundary Coverage

- Module boundary coverage applicable: yes.
- Future feature owner module: not introduced.
- Module owners checked: `src/workbench/projections/read-model/decision-inspector.ts` owns result-review apply de-duplication; `src/workbench/projections/read-model/confirmation-queue.ts` owns selected-demand primary ordering; `src/server/workbench/actions.ts` owns action execution/decision recording.
- Moved responsibilities: removed close/archive side effect from apply action handling.
- Retained facade responsibilities: no broad facade gained new core logic.
- Forbidden write-back locations: no reference project, archive, or generated index was hand-edited.
- Compatibility surface: existing action registry, Workbench snapshots, and approval action shapes are preserved.
- Behavior path tested: apply -> active selected topic -> close confirmation -> archive.
- Follow-up split candidates: none required for this slice.
- Boundary tests or lint checks: targeted Workbench tests plus `npm run lint`.
- Compatibility result: passed.
- Tested with: targeted Workbench suites and product checks.

## Core Mechanism Reuse Coverage

- Core mechanism reuse / architecture growth control coverage applicable: yes.
- Existing mechanisms reused or strengthened: Workbench action registry, scoped action target ids, server revalidation, result review, validation/audit artifacts, source apply, and existing close/archive approval.
- New cross-cutting mechanism and owner: none.
- Why existing mechanisms were insufficient: they were sufficient; the gap was auto-finalization after apply and projection/action ordering.
- Domain-specific logic location: Workbench read-model projection and Workbench server action handler.
- Shared cross-cutting logic location: unchanged.
- Local framework / state machine / projection / validation / gate avoided: no new evidence family, scheduler loop, automation authorizer, or parallel executor was added.
- Public API / facade / Workbench compatibility result: passed existing tests.
- Future-cost reduction result: the usable loop is now represented by real gates rather than another explanatory layer.
- Tested with: targeted Workbench suites and product checks.

## Close / Handoff Drift Coverage

- Close/handoff drift coverage applicable: yes.
- Handoff files checked: `AGENTS.md`, `docs/STATUS.md`, `docs/CURRENT-DEVELOPMENT-PLAN.md`.
- Stale active-path / phase grep: no stale immediate full-auto or old Scheduler next-step phrases found before close; final archive path update is performed during closeout.
- Latest archive / active path alignment: `AGENTS.md` and `docs/STATUS.md` point at `harness/changes/archive/20260621-workbench-usable-manual-closed-loop/summary.md`; no active change remains.
- Pending evolution state checked: no pending Harness evolution after close; `harness-evolve check` passed after close.

## Remote Handoff Acceptance Coverage

- Remote handoff coverage applicable: no.
- Checked provider/repository/action boundary: not applicable.
- Tested with: not applicable.
- If not applicable, reason: change does not affect Draft PR creation/update, PR feedback refresh, provider capability detection, remote checks/reviews, or remote handoff evidence.
